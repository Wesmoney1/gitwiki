# GitWiki
---
GitWiki (just kind of a placeholder name for now) is a wiki hosted entirely on github. It uses a cloudflare worker to retrieve my github PAT token, and allows for users to make accounts.